// controllers/superAdminController.js

const superAdminController = {};

// Placeholder function for SuperAdmin-only data
superAdminController.getAdminData = (req, res) => {
    res.status(200).json({ message: 'SuperAdmin data accessed successfully' });
};

module.exports = superAdminController;
